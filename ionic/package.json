{
  "name": "lpu5-tactical-ionic",
  "version": "1.0.0",
  "description": "LPU5 Tactical – Ionic/Capacitor Mobile App (Android APK + iOS)",
  "keywords": ["capacitor", "ionic", "android", "ios", "tactical"],
  "scripts": {
    "install:deps": "npm install",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "run:android": "npx cap run android",
    "run:ios": "npx cap run ios",
    "build:android": "npx cap build android",
    "build:ios": "npx cap build ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.1.0",
    "@capacitor/android": "^8.1.0",
    "@capacitor/ios": "^8.1.0",
    "@capacitor/geolocation": "^7.1.8",
    "@capacitor/toast": "^7.0.4",
    "@capacitor-community/bluetooth-le": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
